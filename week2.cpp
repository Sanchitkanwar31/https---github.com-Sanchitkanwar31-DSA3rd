#include<bits/stdc++.h>

using namespace std;

int main() {
    // string x, y;
    // cin >> x;
    // cin >> y;

    // // Convert strings to lowercase
    // transform(x.begin(), x.end(), x.begin(), ::tolower);
    // transform(y.begin(), y.end(), y.begin(), ::tolower);

    // // Compare the strings using the compare function
    // int result = x.compare(y);

    // if (result < 0) {
    //     cout << -1 << endl;
    // } else if (result > 0) {
    //     cout << 1 << endl;
    // } else {
    //     cout << 0 << endl;
    // }

    long x,y;
    cin>>x>>y;
    long s= x+y;

    return 0;
}
